# Core Data / Scientific
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0      # Required for Hawkes Process MLE fitting (L-BFGS-B)

# Machine Learning & Deep Learning
torch>=2.0.0       # PyTorch (CUDA supported)

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities
tqdm>=4.65.0

# Optional: Path Signatures formulation (C++ backend, Linux/Mac highly favored)
# Windows users might need to build from source or use the manual fallback.
# signatory>=1.2.6.1.9.0
